import StlParser from "./StlParser.ts";
type CliOptions = {
    ascii?: boolean;
    binary?: boolean;
    type?: "ascii" | "binary";
    readableObjectMode?: boolean;
};
export default function (options?: CliOptions): void | StlParser;
export {};
